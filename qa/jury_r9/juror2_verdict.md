# UI/UX JURY — R9, Juror 2/5 — MOBILE LENS (390×844), verify-and-score

Site: https://cahl.neural-forge.io · Deployed build verified fresh this round: `/api/version` → `{"js":67}`, shell loads `style.css?v=67` + `landing.css?v=67` + `app.js?v=67`, inline `APP_VERSION = 67`, `JS_VERSION = 67` (app.js L32) — version triple in sync. Judged from deployed v67 bytes fetched this round (style.css 88,795 B sha256 9670ff78…; app.js 156,937 B sha256 079d3033…; landing.css 8,928 B sha256 82fb58d0…; index 13,161 B), plus live `/api/today` (1.34 s, 5.2 KB) and `/api/players` (1.45 s warm, 5.61 MB body). My style.css fetch is byte-identical to the sibling copy in qa/jury_r9/ — equivalence chain explicit. Chrome-free per standing directive; 390px lens traced through media queries and shipped rules. Lineage: 7.7 → 8.6 → 8.7 → 9.5 (R6) → 9.6 (R7) → 9.6 (R8) → this round.

**Data context (honesty rule):** preseason thin, unchanged — 2 upcoming games (Brewskies–Trash Pandas 7:40, Whalers–Stepbros 8:50, both Chiller Dublin), `played:null`, no finals, no scores; 27 leagues. Penalized only where design could compensate; empty-state/skeleton termination contracts hold.

## R8 fix verification (my three cited fixes, byte-checked against deployed v67)

| R8 cited fix | Verdict in deployed v67 |
|---|---|
| (1) Move scroll context onto the TABLE (`.card:has(table) table{max-height:65vh}`) + delete the colliding fade `::after` | **SHIPPED — with the correct mechanism this time.** style.css L2110–2116: `@media (max-width:699px){ /* R8-2: the table must be its own scroll container or the sticky th never engages */ .card:has(table) table { display:block; max-height:65vh; overflow-y:auto; } }` — the comment names my R8 root cause verbatim. With the table as the scrollport, the base `th{position:sticky;top:0;z-index:5}` (L509) finally has a real vertical scroller to stick in — the exact pattern that works everywhere else (sticky GitHub-style table headers). The card-level `max-height:70vh` rule is **gone** (zero hits), and the invisible panel-on-panel fade `::after` at the old L2103–2108 is **deleted**; the surviving `::after` at L1930–1941 is the "swipe ›" chip, restored and no longer collision-killed (single same-selector-same-pseudo owner now). Sticky-left first column (L1178–1180) lives inside the same scrollport — both stickies engage in one container. |
| (2) Team inline watchdog → shared `armSkeletonWatchdog` | **SHIPPED.** app.js L1987–1989: `// R8-2: shared watchdog (8s nudge, 40s hard error, debounced retry) — replaces the inline 8s/20s pair that raced api()'s 30s abort` followed by `armSkeletonWatchdog($('#teamContent'), 'your team', …)`. Zero `Timed out after` literals remain in app.js; the only 40000 ms is the shared deadline (L1457) + lookup controller (L375), both exceeding the 30s api() ceiling. The last "timed-out prints while in flight" race is closed at the flagged site. |
| (3) esc() the 2 remaining raw `${data.error}` sites | **SHIPPED — and the class is fully closed.** Census of every error interpolation in app.js: **10 sites, all esc()'d** (L1051, 1300, 1471, 1637, 1704, 2008, 2013, 2317, 2328, 2354 — including both former raw sites L2306→2317/L2317→2328 league paths). Zero raw `${data.error}` / `${e.message}` interpolations remain. |

**Also verified from the round directive:** gate-CTA blue tokenized — `--union-cta: #1f6cb8` (style.css L36, dark) consumed via `var(--union-cta, #1f6cb8)` in landing.css L99; sponsor gold tokenized light-safe — `--sponsor-gold: #b8925e` dark (L37) / `#7a5c2e` light (L100), 3 landing uses each via var(). Cold-index copy widened exactly as cited: app.js L533 now reads `Index cold — first search can take up to ~30s, then it's instant…` (was "10–25s"; real en/em-dashes intact).

## New v67 surface — full marketing landing (verified from bytes)

- **Markup** (index L47–118): sticky `.lp-nav` (brand + 3 anchor links + "Open Dashboard" CTA), hero (HD crest 1490×928, Saira 800 44px title, sub, primary CTA with animated arrow, mono hint line), "What you get" 4-feature grid, About section, League Sponsor card (Upright Creative, 1192×394 lockup, `rel="noopener"`), footer ghost CTA + ChillerStats attribution. Honest, specific copy ("built by a player, for the players", "not affiliated… just fans with a database") — reads like a real product page, not lorem.
- **landing.css** (221 lines, grew 6,510→8,928 B): 3 anchor links hide ≤719px; title 32px ≤719 / 27px ≤430; hero padding tightens; **sponsor card stacks column + centers ≤719px** (L210) as directed. CTA 52px → 50px ≤430 — clears the 44px floor everywhere. `:focus-visible` on links/CTA/sponsor; `prefers-reduced-motion` block (L218–220) kills every transition in the file. Session-skip via `sessionStorage['cahl-entered']`; `gateDismiss` removes the gate after 500ms and focuses `#main` (app.js L12–27).
- **Two material/typography deviations found** (cited under depth/type below): `.lp-nav` uses `backdrop-filter: blur(10px)` (landing L44–45) — the first blur surface outside the palette scrim — and the landing ships its own parallel type scale (12 literal px sizes incl. 11px `.lp-f-icon` and 12.5/13.5px body) plus radius literals (7/8/10/14/16px) against the token system's 6/9/12.

## Standing ledger re-verified in v67 bytes

`concat(finals)` L1214; scored-chip emit + grid CSS; `.scrim` CSS; accent `#ef3d54` 4.80:1 panel / 5.13:1 bg (recomputed); faint `#7d92ad` 5.78:1 panel / 6.18:1 bg; muted `#8494ab` 5.97:1 panel / 5.10:1 panel-3; rink-head `#4a9ae6` 6.19:1 panel; light-theme `--sponsor-gold #7a5c2e` 5.30:1 inset; tonight-strip 2×2 at ≤430 (L2118); coarse `button.small{min-height:44px}` (L2121); autoToggle `:has(:checked)` feedback (L250–251); count-up RM gate (L799); RM blocks L1215/1295/1401 + landing L218; api() 30s AbortController + silent retry (read in full, L~824–900); mono tokens 9.5/10.5/12px (L2017–2019); **zero font-size literals in app.js**; `tabular-nums` ×40 sites incl. tables; release-gate grep (min-height/width:0 inside max-width media) — every hit is a flex text-guard or sits in `min-width:900px` desktop blocks; **no tap-floor regression class recurrence**. Token-aware hex census: the only raw literals outside token definitions are `#a30e1e` (light `.lead-pts`, an intentional AA retint of the accent), `#4a9ae6` (documented rink-head AA comment), and two hexes inside a contrast-documentation comment — clean. Token drift ledger carry (localStorage comment in landing.css L4) **persists**; `.sponsor-strip` styles still live in landing.css; 17 inline `margin-top` emissions unchanged.

**/api/players:** 1.45 s warm single-shot but **5.61 MB uncompressed body** — the standing server-side carry (gzip/paginate), outside UI scope, unchanged since R8.

## Scores (fresh, at 390px)

| # | Dimension | Evidence at 390px | Score |
|---|---|---|---|
| 1 | Visual identity | The landing is now the first touchpoint and it's a full brand page: HD crest at min(340px,78%)≈271px on a 390px screen, rink-line margins whispering behind, union/red radials both themes, Saira 800 "Every Game. Every Stat. One Dashboard.", sponsor lockup in the system's gold. Dashboard identity unchanged and audited token-clean. Distinctly hockey end-to-end, from first paint to footer. | 9.7 |
| 2 | Typographic system | Dashboard side holds: 8 px sizes at original rule bodies, zero app.js literals, mono tokens 9.5/10.5/12, Saira 600/700/800 loaded, tabular-nums ×40. But the landing introduces a **parallel scale**: 12 literal sizes (11px f-icons, 12.5/13.5px body, 15/17px) that bypass the mono/display tokens — an 11px literal in a system whose floor token is 9.5px and whose steps are 9.5/10.5/12. No faux-bold, nothing below 9.5, no zoom risk; it's scale discipline, not legibility. | 9.6 |
| 3 | Color system | Re-measured both themes: accent 4.80/5.13; gold/inset **6.76 dark, 5.30 light** (now tokenized, light-safe as directed); CTA white-on-#1f6cb8 5.39; muted/faint ≥4.91 everywhere; rink-head 6.19. Sole sub-AA text in the product remains `.pill-count` 10.5px at opacity .7 (L601) — decorative count suffix, last of its tier. Both new surfaces AA-clean. | 9.7 |
| 4 | Layout & hierarchy | Landing reads top-to-bottom like a product page: nav → hero → 4-feature grid → about → sponsor → footer CTA, hero capped at 860px, sponsor card stacks and centers ≤719px, links yield to the CTA <719. Dashboard rhythm unchanged: tonight-strip 2×2, by-rink grouping, scored-chip two-line anatomy, sponsor strip as quiet footer band. Single-rink slate renders one clean Upcoming card. | 9.7 |
| 5 | Density & data presentation | **The 3-round anchor is closed with mechanism.** The table is now its own scrollport (`display:block;max-height:65vh;overflow-y:auto` ≤699px, L2113–2115) so `th{position:sticky;top:0}` (L509) finally engages — headers persist over 65vh of rows at 390px; the invisible fade is deleted and the "swipe ›" chip is the sole `::after` owner again (L1930–1941); sticky-left col + zebra + 44px cell min-widths + print discipline all re-verified; the fix comment cites the root cause. Residual nit: data rows ≈40px tall (sub-44, but rows aren't controls) — with effort, nothing defect-grade remains at this viewport. | 9.7 |
| 6 | Interaction & micro-feedback | Every path terminates: api() 30s AbortController + silent retry (read in full), shared 40s watchdog + debounced retry on League/**Team**/Analytics, PTR, palette, autoToggle feedback. One defect found with effort: the gate is `role=dialog aria-modal` but has **no Escape handling, no focus trap, no `inert`/aria-hidden on #app** (Escape handlers exist at L111/762/2677/3150 — none cover the gate; zero `inert` refs) — keyboard users get one exit: Enter. The R8 interaction defect list is otherwise fully closed. | 9.6 |
| 7 | Motion design | Inventory functional-only: LIVE pulse, score-flash, capped entrance, PTR spinner, gate fade, CTA arrow nudge, sponsor hover. RM coverage spans both files (style L1215/1295/1401 + landing L218–220 covering gate/CTA/arrow/sponsor/links); count-ups RM-gated; hero drop-shadow static; no decorative motion found in the new surface. | 9.7 |
| 8 | Depth & material | App shell ladder intact (bg→panel→panel-2→panel-3→inset, hairlines, no fake shadows). One real deviation: `.lp-nav` ships `backdrop-filter: blur(10px)` — the marketing funnel's first blur surface, off the no-blur material system (style.css's only blur remains the palette scrim, 3px). Solid color-mix at 82% already carries the nav; the blur is removable without visual loss. Gate card/sponsor surfaces otherwise inherit the material system correctly. | 9.6 |
| 9 | Consistency & component quality | FIX 4 landed: `--union-cta`/`--sponsor-gold` tokens defined and consumed via var() in both themes — the 4th-blue and raw-gold one-offs are gone. But the rest of my R8 fix #3 was not delivered: `.sponsor-strip` styles still live in landing.css (app-wide placement drift), the localStorage→sessionStorage comment drift persists (landing L4), 17 inline `margin-top` emissions unchanged, and the landing adds radius literals (7/8/10/14/16px vs --radius 6/9/12) + a parallel type scale. Tokens closed; system discipline on the new surface is partial. | 9.6 |
| 10 | Mobile ergonomics | Tap-floor census at 390px clears: pills 44, kpalBtn 44, nav 48, `button.small` 44, gate CTA 52→50 ≤430, swipe chip pointer-events:none (decorative, exempt), lp-links hidden <719 (their 38px height never renders at 390). 16px inputs (no iOS zoom), safe-area insets, PTR, gate/strip focus-visible. Sponsor card stacks column at 390 with 170px lockup — clean. Release-gate grep clean. No new sub-44 control; the R8 residue list is empty at this lens. | 9.7 |

**WEIGHTED TOTAL: 9.7**/10
(identity 9.7×.12 + type 9.6×.12 + color 9.7×.10 + layout 9.7×.12 + density 9.7×.12 + interaction 9.6×.10 + motion 9.7×.08 + depth 9.6×.08 + consistency 9.6×.08 + mobile 9.7×.08 = **9.662** → 9.7)

Gate check: the release bar is ≥9.7 per dimension. The weighted total clears 9.7 for the first time (9.638 → 9.662), and the density anchor — three rounds carried — is closed with the correct mechanism. But **four dimensions sit at 9.6** (type, interaction, depth, consistency), each with one named defect above. All movement this round is byte-verified, not claimed.

## The 3 cheapest fixes toward 9.7-per-dimension

1. **De-blur the landing nav + snap landing radii to tokens (depth 9.6→9.7, consistency credit, ≈+0.008).** Delete `backdrop-filter/-webkit-backdrop-filter` from `.lp-nav` (landing L44–45) and raise the color-mix to ~96% or swap to `var(--panel)` + existing hairline — the nav already has a border-bottom, so the blur buys nothing at 390px. In the same file, replace the six radius literals (7/8/10/14/16px) with `var(--radius-sm/md/lg)`. Two-block edit, zero risk. Est. delta +0.008 (depth) + consistency credit.
2. **Give the gate its keyboard contract (interaction 9.6→9.7, ≈+0.010).** It already declares `role=dialog aria-modal`; make it true: one `keydown` listener — Escape → `gateDismiss()` — plus `document.getElementById('app').inert = true` while the gate is shown (and false in gateDismiss). ~6 lines at a site already named (app.js L11–27). Est. delta +0.010.
3. **Finish the landing system-alignment sweep (consistency 9.6→9.7, type 9.6→9.7, ≈+0.020).** (a) Hoist `.sponsor-strip` rules from landing.css into style.css so the dashboard component lives with its system; (b) fix the one-word comment drift (localStorage→sessionStorage, landing L4); (c) map the landing's 11px f-icons and 12.5/13.5px body onto the mono/display tokens (or define landing tokens once at the top of landing.css); (d) convert the 17 inline `margin-top` emissions to `.mt-*` utilities. All mechanical. Est. delta +0.008 (consistency) + +0.012 (type).

Post-fix estimate: 9.662 + 0.008 + 0.010 + 0.020 = **9.700**, with every dimension at the 9.7 gate. Beyond that, the residual is the preseason data ceiling and the 5.61 MB `/api/players` body (warm 1.45 s, but cell-network cold loads pay full weight) — server-side gzip/paginate, outside UI scope.

VERDICT: 9.7/10
