# CAHL UI/UX JURY — R9, Juror 3/5 — VISUAL CRAFT lens (v67) — verification round

- Method: Chrome-free static audit of deployed bytes — `/static/css/style.css?v=67` (88,795 B, sha `9670ff78`), `/static/js/app.js?v=67` (156,937 B, sha `079d3033`), `/` index (13,161 B), `/static/css/landing.css?v=67` (8,928 B), `/static/img/cahl-logo.svg` (2,936 B), `cahl-logo-hd.png` (129 KB), `upright-lockup.png` (242,816 B) — fetched 2026-09-04 ~20:05Z; `/api/version` = `{"js":67}`; `/api/today` live 200 in 1.43s (2 games, 0 stale Bye-Week/Team-Blue/Red rows). style.css byte-identical across two independent fetches. Baselines: my saved `jury_r8/style_v63.css` (`e34cf33d`) + `jury_r8/landing_v63.css` + `jury_r8/app_v63.js` → +64/+190/+308 diff lines. Contrast recomputed independently (WCAG relative luminance); Upright lockup decoded at the PNG pixel level (full unfilter + re-encode + visual confirm — my first decode pass had a Paeth-predictor bug and produced phantom hues; corrected decoder, exact-color census `#ccc4b4` cream / `#947b58` bronze, and vision check all agree with the brief).
- Score lineage: 7.8 (R2) → 8.1 (R3) → 8.5 (R4) → 8.7 (R5) → 8.7 (R6) → 9.1 (R7) → 9.4 (R8) → **9.5 (R9)** — every R8-cited fix landed, plus a full landing-page rewrite; the gain is real, but the rewrite minted new type-scale debt that caps the round.

## Fix-verification ledger (my R8 citations → v67 deployed bytes)

| R8 citation | v67 byte evidence | Status |
|---|---|---|
| Last inline h3 → `h3.section-h` | `session-card` renders `<h3 class="section-h tight">Session records</h3>` (app.js L2072); `h3.section-h.tight { margin: 0 0 8px; }` shipped (style.css L442). Same 8px rhythm, now on the system | LANDED ✓ |
| Watchdog comment drift (said 25s, code 40s) | comment now reads "hard-error deadline (40s) must exceed api()'s 30s ceiling + retry-once" (app.js L1437) — numbers in the comment match the code | LANDED ✓ |
| Duplicate light-theme `.rink-head` rule | **STILL PRESENT** — `html[data-theme="light"] .rink-head { color: var(--union-deep); }` appears twice, adjacent, style.css L1719–1720. Harmless (identical decls) but it is dead weight I flagged verbatim last round | NOT FIXED ✗ |
| landing.css off-scale 15/19/25px | old values gone — but the full landing **rewrite** ships a new off-scale set: 13 decls off the 8-step scale (11, 12.5×2, 13.5×2, 15, 17×3, 19, 27, 32, 44) of 21 total; dashboard style.css census remains exactly 8 px values (13/14/16/18/20/26/30/34), 73 decls, zero sub-13, zero JS literals | PARTIAL — old strays fixed, rewrite minted 13 new |

New work verified this round (unrequested): full marketing landing replaces the gate-card (sticky brand nav + hero crest + 4-feature grid + about + sponsor + footer); `--union-cta` #1f6cb8 and `--sponsor-gold` promoted to real tokens with light-theme variants (#7a5c2e — my own math: 6.18:1 light panel, 6.42:1 dark panel; CTA idle 5.39:1, hover 3.40:1 on a 19px bold = large-text OK); rink-lines backdrop narrowed to 7%/93% margin hairlines, clear of every text column; em-dash in the hero sub is nbsp-bound (`hockey&nbsp;—`, count 1, no orphan); Upright serif lockup is true-alpha RGBA (72.5% transparent), cream #E6DFCE + bronze per pixel census and visual check; scrim cards stripped of LIVE material at the CSS layer (`[data-scrim="1"]` kills border/glow-bar/text-shadow with `!important`, closing R8-1's sibling-selector hole); palette overlay spinner added to the reduced-motion kill-list; mobile table scroll-context moved onto the table (`.card:has(table) table { display:block; max-height:65vh; overflow-y:auto }`) — R8-2's mechanism critique answered; Team tab's inline 8s/20s watchdog replaced by the shared `armSkeletonWatchdog` (kill-app.js-L1984); dead-team self-heal clears stale localStorage and re-opens the picker; `esc()` census now **zero** raw interpolations (all 6 R8 sites fixed: L1637/1704/2317/2328/2457 + Analytics); dataStamp now repaints every 60s (tick, not freeze — 6-round carry closed); all 3 gate entry points (`gateEnter`/`gateEnterCta`/`gateEnterFoot`) share one `gateDismiss`.

Also re-verified intact: 20 tabular-nums, 6 zebra rules, 14 focus-visible, 6 min-height-44px floors, 6 coarse-pointer blocks, 4 RM blocks, 10 keyframes all purposeful, version triad aligned (JS 67 = APP_VERSION 67 = `/api/version`). `.rink-head` #4a9ae6 re-measured 6.19:1 panel / 6.62:1 bg.

## Dimension scores (fresh, v67)

| # | Dimension | Evidence (v67 bytes) | Score |
|---|---|---|---|
| 1 | Visual identity | Landing is now a real marketing surface — sticky brand nav, 340px hero crest, mono chip icons (LS/ST/AT/BR) in tokenized bronze, sponsor section with the verified serif lockup, footer CTA; dashboard identity (rink watermark, broadcast bug, tonight-KPI band) untouched. Remaining: CAHL's own mark is still crest-only; the signature serif moment belongs to the sponsor | 9.5 |
| 2 | Typographic system | Dashboard scale pristine (8 values, 73 decls, zero JS literals); but the landing rewrite ships 13 off-scale decls incl. sub-13px return (11px icons, 12.5px fine print) and zero tokens for its sizes — new debt, first type regression since R6 | 9.3 |
| 3 | Color system | Sponsor gold is now a two-theme token pair, both AA by my math (6.42 dark / 6.18 light on panel); CTA idle/hover tokenized; Goal-Red discipline intact; remaining: 4 raw hex fallbacks in landing.css (`#1f6cb8`,`#3a8fe0`,`#b8925e`,`#ffffff`) and the gold hover state on white CTA (`--union-hover` 3.40:1) is large-text-only legal | 9.6 |
| 4 | Layout & hierarchy | 7%/93% rink-lines margins clear all text columns (verified in the gradient math); lp-grid `auto-fit minmax(210px,1fr)`; sponsor card stacks ≤719px; remaining: the duplicate `.rink-head` light rule (L1719–1720) — the one verbatim unshipped item | 9.6 |
| 5 | Density & data presentation | Same core bytes as v63's 9.3 (sticky theads, zebra, 20 tnum, ellipsis discipline) with no new density defects; scored-strip and tonight-strip untouched | 9.3 |
| 6 | Interaction & micro-feedback | esc() census zero across every error path; watchdog ladder honest (comment now matches 40s code); dataStamp ticks every 60s; dead-team self-heal answers a real dead-end. Remaining: palette input focus ring still suppressed with no `:focus-within` compensation (3rd round); cold-index copy "up to ~30s" vs my measured 33.3s cold /api/players (5.4MB, ungzipped) — band still marginally falsifiable | 9.5 |
| 7 | Motion design | Palette spinner is the only new animation and it's already RM-gated; landing.css adds zero keyframes and RM-blocks all 7 of its transitions; 10 keyframes, all purposeful. Not 9.7: timing feel remains photon territory | 9.6 |
| 8 | Depth & material | Surface stack strictly ordered; scrim cards now stripped of glow/wash at CSS level; text-shadow census is exactly the 2 live-score sites + the scrim kill. Remaining: lp-nav adds a second `backdrop-filter` (blur 10px) — tokenized-adjacent but a new blur surface outside the `.pal-ov` singleton budget | 9.4 |
| 9 | Consistency & component quality | section-h.tight adopted; all three CTAs one component; feature chips reuse mono-kicker anatomy; sponsor hover retokened off raw hex. Remaining: 43 inline `style=""` spacing one-offs in JS templates (margin-only, but the pattern section-h was built to kill); dup rink-head rule | 9.5 |
| 10 | Mobile ergonomics | Table-as-own-scroll-container fixes the sticky-th mechanism (R8-2 critique landed); landing collapses cleanly (nav links hidden ≤719, CTA 50px ≤430, sponsor card stacks); 6× 44px floors + coarse-pointer blocks intact; 390px photons remain unverified (static) | 9.6 |

**WEIGHTED TOTAL: 9.5/10** (identity 1.140 + type 1.116 + color 0.960 + layout 1.152 + density 1.116 + interaction 0.950 + motion 0.768 + depth 0.752 + consistency 0.760 + mobile 0.768 = **9.482**)

Gate status: ≥9.7 per-dimension still met by **zero** dimensions (four at 9.6). Every R8-cited fix landed except the duplicate `.rink-head` rule, and the landing rewrite traded a gate-card for a full page — at the cost of a fresh type-scale census problem in landing.css.

## The 3 cheapest fixes toward 9.7-per-dimension

1. **Quantize landing.css onto the dashboard type scale** (~30min): map 17→16 (×3), 15→14, 13.5→14 (×2), 12.5→13 (×2), 19→20, 27→26, 32→34, and either declare `--fs-hero: 44px` or pull the hero to 34; move 11px chip icons to a declared token. Type 9.3→~9.6, consistency 9.5→~9.6. **Delta: ~+0.03.**
2. **Delete the dead duplicate + close the color strays** (~15min): remove the second `html[data-theme="light"] .rink-head` (L1720); delete the 4 raw-hex fallbacks in landing.css (it loads after style.css, so the vars always resolve); add `.pal-input-row:focus-within { border-bottom-color: var(--union) }` + accent the search glyph so the de-styled input answers focus (3rd round carrying this). Layout 9.6→~9.7, interaction 9.5→~9.6. **Delta: ~+0.035.**
3. **Make the cold-index copy true or the payload small** (~1h, server-side): /api/players measured 33.3s cold, 5.40MB wire, no gzip this round — either gzip+trim the payload (R8-A carry, 4th round) or widen the copy to "up to ~40s". Also drop lp-nav's `backdrop-filter` for the dashboard's solid-border nav idiom (restores the single-blur budget). Interaction →~9.6, depth 9.4→~9.5. **Delta: ~+0.02.**

Combined realistic ceiling for R10 if all three land: **~9.55/10 weighted, five dimensions at 9.6–9.7**. The honest gap is unchanged: hover/press feel, animation timing, and true 390px rendering need vision_analyze on owner-captured screenshots — my Chrome-free audit certifies bytes, not photons.

VERDICT: 9.5/10
