# CAHL UI/UX JURY — Juror 3/5, Round 2 (VISUAL CRAFT lens) — v51

Site: https://cahl.neural-forge.io (app v51, /api/version {"js":51}; /static/css/style.css?v=51 = 80,134 B, /static/js/app.js?v=51 = 2,908 L).
**Method note (static):** per owner directive this round was scored from source, not a driven browser (Chrome remote debugging disabled by owner): full CSS + app.js + HTML fetched, contrast computed from hex pairs, Google Fonts css2 response checked. Per-dimension verdicts are grounded in the shipped CSS/JS/HTML; live-paint behavior is marked `(static)`.
R1 verdict for reference: 6.5/10 on v50.

## Round-1 fix verification (claimed → found)

| Claimed fix | Verdict | Evidence |
|---|---|---|
| Zebra on leaderboard + standings, BOTH themes | **SHIPS (static)** | `table.board / #leagueSecStandings / tr.roster-player :nth-child(even)` → `color-mix(in srgb, var(--panel-2) 40%, transparent)`, `:hover` re-solidified. Panel-2 is theme-scoped: dark `#131b2b` over `#0e1420` panel, light `#f2f5fa` over `#fff` — both tints verified in token blocks; text contrast survives (see type row). JS emits `class="board"` and `roster-player` rows, so selectors bind. |
| Mono scale snaps to 3 steps (`--mono-xs/sm/md`) | **HALF** | Tokens exist (`9.5/10.5/11.5px`) and are applied to `.eyebrow/th/.status-chip/.sb-meta/.pal-grp`; but `th` keeps an earlier 10px rule (cascade), `.sb-meta` an earlier 10px (→ 9 mono sizes remain, 8.5–11.5px), and the two non-JetBrains mono-adjacent items: nav link captions 10px Inter (display, not mono), `.brand-sub` 8.5px→7.5px mobile. |
| Desktop Today header labels over BOTH groups | **SHIPS (static)** | Head spans `1 / -1` with an 11-track template (58/1.5fr/118/1.5fr/108 + 16px seam + mirror); row content width `(W−24)/2−12` matches group width `(W−696)+.../5fr` exactly, verified arithmetically for 1440/1280/1024. Group-B labels repeat via `::after` pseudos. Stray 16px vs 56px track mismatch between head and row is sub-pixel inside fixed 118px Score col. |
| One accent discipline, no purple/amber strays | **PARTIAL** | Zero purple/pink anywhere (grep clean; R1's blue→purple→red stripe now `var(--union)→var(--accent)` only; palette-input pink tint gone). BUT dark theme still ships 5 off-CBJ status hues — mint `#2fd08c` W-chips/timeline, orange `#f0a24b` OTL + `.my-team-star` amber star, bright red `#ff5d6e`, plus light-theme `.my-team-star` `#b26a00`. The amber "My Team" star chip R1 flagged is still amber. |
| Saira Condensed renders on scores | **YES (static)** | `.t-score/.sb-num` + 26 more `var(--display)` hooks, incl. new `.jersey-num` 16px; html head loads Saira Condensed 600/700 and Google css2 serves it; no CSP blocks fonts; today-row team names 16.5px Saira too. Caveat: only `document.fonts` in a live browser can prove paint (static) — every DOM hook is in place. |
| `.link` → text-2 + hover underline | **SHIPS (static)** | `.link { color: var(--text-2) }`, `:hover { color: var(--text); underline }`; off-token link-blue gone. `--union: #2a7fd4` remains as the dark-theme "Union Blue, luminance-lifted" token (light: #0057b8) — a named token, not a stray. |
| ppg-bar median tick | **SHIPS (static)** | `.ppg-bar::after` 1px tick at 50%, `--faint`, z-index above fill under value; light-theme border variant. |
| Mobile header/labels | **SHIPS** | `.today-cols-head{display:none}` <600px (kills "TimeHomeScoreAwayRink"), rows stack to real cards; footer outside nav + `main` padding-bottom 72px+safe-area. |

**6 of 8 confirmed, 1 half, 1 partial.** Genuinely earned: ~1.2 net.

## Per-dimension scores

| # | Dimension | Evidence (what I saw) | Score |
|---|---|---|---|
| 1 | Visual identity | Broadcast skeleton is real and stronger: 2px header stripe strictly union→accent, JetBrains Mono micro-labels at disciplined tracking, Saira Condensed scores + jersey numerals, rink pills, LIVE pill, standings PLAYOFF LINE. But marquee is still type-led, not image-led: the dark hero surface is an inset wash + a glow that R1-F sets to `background:none` (halo only); on a season-start slate the only display-face numerals visible on Today are the mono "vs" column. A broadcast product is recognizable by its art direction under real content; v51 has the typographic bones but no signature art/visual moment (no rink canvas, no marquee imagery, hero is an indigo-tinted card). Effort: audited every display/mono hook and identity element in 80KB CSS. | 7.8 |
| 2 | Typographic system | Big cleanup landed: display face reaches scores/sb-num/team rows/jersey numerals; `table { tabular-nums }` global + right-aligned `.num` columns + right-aligned sb-num; global contrast lift (muted/faint ≥4.5:1 both themes, computed: 10.45/9.99 dark panel/zebra, 7.49/6.93 light). Defects: mono scale still not 3 sizes — the fix pass defines `--mono-xs/sm/md` but `th`/`.sb-meta` keep earlier 10px rules, so live mono sizes are 8.5/9/9.5/10/10.5/11/11.5 (9 values incl. contextual 9px swipe chip); `.brand-sub` 8.5px→7.5px at 390px is below micro-label floor; kbd chips 10px still scatter. | 7.6 |
| 3 | Color system | Accent discipline is structurally fixed: red counted only on live/cut/active/LIVE pill/danger; header stripe strictly union→accent; zero purple/pink (R1 strays dead); `.link` off-token blue gone. Remaining breaches: dark theme form chips/timeline use mint #2fd08c + red #ff5d6e (off-CBJ status hues, light theme correctly uses CBJ-adjacent #0e8a56/#ce1126); amber OTL #f0a24b + `.my-team-star` amber star chip still there (R1 item, still open); light `--win #0e8a56` is a green the broadcast palette never uses. Red on dark panel = 4.18:1 (passes only as large/graphic text). | 7.2 |
| 4 | Layout & hierarchy | R1's structural break is genuinely repaired: today header spans both groups on desktop with verified-perfect column math at 1440/1280/1024; mobile gets real stacked cards and a hidden (not smashed) header; hero name is flow-restored with wrap guards. Marquee/long-tail hierarchy exists (hero + LIVE pulse vs calm tables) but the hero band still reads as a stretched admin card — indigo tint, one star chip, no scoreboard art. Light theme monochrome wash reduces scan contrast between cards. | 7.9 |
| 5 | Density & data presentation | Best dimension. Zebra + hover interplay correct in both themes, sticky theads, sortable board with arrow, PLAYOFF LINE divider, mobile min-width guards + "swipe ›" affordance, tabular numerals everywhere, GD dot-axis with labels on own row (R1 "Nomads0" fusion root-caused and fixed), auto-select league kills the R1 empty-League dead end. Withheld from 9: mono-scale scatter persists inside these tables' labels; still no visually verifiable evidence of a live paint (static round). | 8.3 |
| 6 | Interaction & micro-feedback | From source: 180ms debounced typeahead with stale-error clearing (R1 spam fixed), skeletons (`.skeleton-card`) on tab loads, toast system, palette keyboard model, retry affordances on errors, refresh-disable pattern, hash deep-links restored. All real code paths — but this round could not exercise a single one in a live browser (static); score reflects verified implementation, not felt latency. | 7.6 |
| 7 | Motion design | 11 keyframes, all functional (live pulse, shimmer, toast, theme icon, score flash, entrance fade); 3 reduced-motion blocks incl. live-badge/chip animation kill; the decorative R1 glow is now a halo on the name; GD/ppg visuals are static-ink (reduced-motion safe by construction). Nothing decorative found in source. Under-animated remains true: no differentiation between tab transitions, count-ups exist in JS but subtle. | 7.8 |
| 8 | Depth & material | Exemplary flat system: 1 backdrop-filter in 80KB CSS, hairline borders, two shadow tokens (soft/pop), theme-aware inset/panel-2/panel-3 stack, zebra as luminance tint not gray paint, R1-E "swipe" fade uses `background-attachment: local/scroll` two-layer trick. No blur soup, no fake shadows. Only lapse: `--shadow-pop` at 44px blur is heavy for a flat broadcast material (palette pop only). | 8.0 |
| 9 | Consistency & component quality | One scorecell anatomy now enforced (`.t-score` inset chip, `.sb-num` display face, jersey numerals); chips/pills/badges from one radius/border system; UPCOMING chips moved off rows to bare "vs"; "UPDATED HH:MM" copy bug fixed (24h-safe, no "tonight"); rink names normalized (NTPRD/OhioHealth prefixes stripped). Holding it back: the 9-value mono size scatter IS the component-quality tell (labels don't share one rhythm); `<select>` still gets only generic input styling (native chevron, no custom face) — R1 leftover, still open; dark status-hue set differs from light (two color grammars for W/L/OTL chips). | 7.3 |
| 10 | Mobile ergonomics | R1's worst surface is now solid: stacked game cards (time+board line, full-width home/away, rink pill), broken concatenated header gone, footer with data stamp outside nav + 72px+safe-area clearance (overlap bug fixed), header cluster wrap guards, icon-only search <430px, ≥44px table cells and nav targets, zebra reads on mobile panels. Residual: brand-sub drops to 7.5px at 390px (sub-legible micro-label), swipe-fade chip at 9px mono. | 8.0 |

## WEIGHTED TOTAL
identity 7.8×.12 + type 7.6×.12 + color 7.2×.10 + layout 7.9×.12 + density 8.3×.12 + interaction 7.6×.10 + motion 7.8×.08 + depth 8.0×.08 + consistency 7.3×.08 + mobile 8.0×.08 = **7.8/10** (computed 7.760)

## The 3 cheapest fixes with highest score delta

1. **Finish the mono scale (delete the cascade strays).** Remove `font-size: 10px` from the base `th` (line 490) and base `.sb-meta` (line 386) so the R1-F tokens actually win; snap `.brand-sub` (7.5px mobile / 8.5px desktop) and the two 9px chips to the nearest token. One small CSS diff, no layout risk. Delta: type +0.8, consistency +0.6 → **+0.17 total**.
2. **Unify dark status hues to the CBJ set.** Map `--win #2fd08c→#0e8a56`, `--loss #ff5d6e→#ce1126 family`, keep --otl only if it becomes a CBJ silver/white treatment; recolor `.my-team-star` amber → Union Blue tint (light + dark). Token-level replace + the rgba() tints in form-chip/streak-badge/timeline rules. Delta: color +1.0, identity +0.4 → **+0.19 total**.
3. **Give the hero one broadcast art moment.** The structure is fixed; what's missing is a signature visual: subtle rink-lines/center-circle SVG watermark or a diagonal-cut accent panel behind the hero team name (CBJ hues, opacity ≤0.10, reduced-motion safe, no new hues). Delta: identity +1.0, layout +0.3 → **+0.17 total**.

Sum of cheap fixes ≈ +0.5 → ~8.3 ceiling for the craft lens without: real content paint verification (fonts/zebra live), a signature data-viz layer on Today, and the native `<select>` face.

*Static-round caveat: dimension 6 (interaction) and the paint-claims inside 1/2/5 are source-verified, not browser-verified; a live pass could move type ±0.3 and interaction ±0.5 either way. Screenshots were not captured (browser automation disabled by owner mid-round).*

VERDICT: 7.8/10
