# UI/UX JURY — R8, Juror 2/5 — MOBILE LENS (390×844), verify-and-score

Site: https://cahl.neural-forge.io · Deployed build verified fresh this round: `/api/version` → `{"js":63}`, markup loads `style.css?v=63` + `landing.css?v=63` + `app.js?v=63`, inline `APP_VERSION = 63`. Judged from deployed v63 bytes fetched this round (style.css 88,121 B sha256 e34cf33d…; app.js 156,017 B sha256 197f8c28…; index 9,964 B; **landing.css 6,510 B — new file, fetched and read in full**), plus live `/api/today` and a timed `/api/players`. Chrome-free per standing directive; 390px lens traced through media queries and shipped rules. Lineage: 7.7 → 8.6 → 8.7 → 9.5 (R6) → 9.6 (R7) → this round.

**Data context (honesty rule):** preseason thin, unchanged — 2 upcoming games (Brewskies–Trash Pandas 7:40, Whalers–Stepbros 8:50, both Chiller Dublin), `played:null`, no finals, no scores. 27 leagues in the payload. Penalized only where design could compensate; empty-state/skeleton termination contracts hold.

## R7 fix verification (my three cited defects, byte-checked against deployed v63)

| R7 cited fix | Verdict in deployed v63 |
|---|---|
| (1) Mobile table scroll context: `.card:has(table){max-height:70vh;overflow-y:auto}` + sticky bottom fade at ≤699px | **SHIPPED — but mechanism-neutral; two sub-defects found.** Block verified at style.css L2098–2109 (`R7-2 — MOBILE TABLE SCROLL CONTEXT + KPI 2x2` banner, sticky `::after` fade L2103–2108). Traced the scroll architecture: base rule L485–492 keeps `table{display:block;overflow-x:auto}` **unconditional** below the `@media(min-width:900px)` revert (L510) — and per CSS Overflow §3, `overflow-x:auto` computes `overflow-y:auto`, making the **table** the nearest scroll container for `th{position:sticky;top:0}` (L500). With `height:auto` the table never scrolls vertically, so the header never engages; the card's 70vh scroll instead pushes the *entire table, header included*, out of view. Exactly the root cause I cited in R7, still unfixed — the context was added to a container that is not the scrollport. Worse, the appended `.card:has(table)::after` (same selector + pseudo as the "swipe ›" chip rule at L1921, later in cascade, same media condition) **wholesale replaces the horizontal-scroll affordance chip** at ≤699px — and paints `linear-gradient(to top, var(--panel), transparent)` on the card's own `--panel` background, i.e. **invisible**: the fade is a no-op and the chip is dead. Density does not advance. |
| (2) `tonight-strip` 2×2 at ≤430px | **SHIPPED — verified effective.** L2110–2112 inside `@media (max-width:430px)`: `grid-template-columns:repeat(2,1fr)`. At 390px: ≈156px cells (354 viewport − 36 card padding − 6 gap, ÷2), `tonight-num` 20px (≤599 rule L1714), labels `--mono-xs`. The R7 "tightest grid on the page" nit is closed; this also finally makes my mis-credited R6 evidence line true. |
| (3) Coarse-pointer floor for `button.small` | **SHIPPED — verified.** L2113–2115 inside `@media (hover:none),(pointer:coarse)`: `button.small{min-height:44px;padding:10px 14px}` (base L539: 6px/11px). All emitters are `<button class="ghost small">` retry/CTA buttons — inline-block, min-height applies. Release-gate grep re-run: zero `min-height/width:0` inside any max-width media block (the only hits are `min-width:0` flex text-guards and L1111 inside `min-width:900px` desktop). No tap-floor regression class recurrence. |

## New v63 surfaces (verified from bytes + fetched assets)

- **Landing gate** (index L47–64 + landing.css, 175 lines): `#landingGate` fixed overlay z-200, `role=dialog aria-modal`, HD logo (HEAD 200, 129 KB), rink-lines `::before` backdrop echoing the hero watermark (blue/red hairlines + 120px center ring, whisper alpha, light-theme dimmed), title 30px Saira 800 (25px ≤479), CTA `#1f6cb8` with white text **5.39:1 (my calc; their comment says 5.4 — honest)**, 52px min-height, hover/active/focus-visible states, focus moves to `#main` on enter (app.js L24), session-skip via `sessionStorage['cahl-entered']` (L12–16), `prefers-reduced-motion` block covers all gate transitions (landing.css L172–174). Brand moment, on-system.
- **Sponsor strip on Today bottom** (app.js L1235–1239, styles landing.css L139–161): honest disclosure pattern ("Official Sponsor" mono-xs kicker + name), `--inset` well, hairline, focus-visible, hover gold, ≤479 tune, RM covered; assets 200 (crown 128 KB). Note: dashboard component styled in the *landing* stylesheet (loaded app-wide) — placement drift, cited under consistency.
- **Cold-index copy**: exact string verified at app.js L531 — `Index cold — first search takes 10–25s, then it's instant…` — in the typeahead fallback when local hits are empty and the index is loading (L528–531). Honest, conditional, real en/em-dashes. Context: `/api/players` measured **27.5s** this round (5.6 MB payload) — copy slightly optimistic at the top end; server payload size remains the standing backend carry.

**Standing ledger re-verified in v63 bytes** (R7 verification debt): `concat(finals)` L1212; scored-chip emit L1202 + grid CSS L1692–1703; `.scrim` emit L1148–1154 + CSS L445–446; accent `#ef3d54` (my calc 5.13:1 bg / 4.76:1 panel); faint `#7d92ad`/`#5d6b80` with cascade-end override L1873–1878 (5.74:1 panel computed); **rink-head `#4a9ae6` L1708 = 6.15:1 on panel — the 2-round carry is CLOSED**; `#3a8fe0` survives only as the `--union-hover` token definition (zero raw uses); `#e8253c` extinct; Saira Condensed 600/700/800 + JetBrains Mono in the fonts href; hasOpp guard on team `recent_result` L2102–2105; `tr:focus-within` hover parity L504 + `tr.link:focus-visible` L1381 (R7-3 carry closed); shared watchdog upgraded to **40s hard deadline + debounced retry** (L1433–1457) now exceeding api()'s 30s+retry (R7-4 race closed *on the shared helper*); RM blocks L1206/1286/1392.

## Scores (fresh, at 390px)

| # | Dimension | Evidence at 390px | Score |
|---|---|---|---|
| 1 | Visual identity | The gate is now the first touchpoint and it's a brand moment: HD CBJ-system logo, rink-lines backdrop quoting the hero watermark, union/red radial glows both themes, Saira 800 headline. Dashboard identity unchanged and audited clean — hex census: zero off-token literals in style.css (old hover/accent hues exist only as dead-or-token definitions). Live-pill grammar, 30–34px scoreboard numerals. Distinctly hockey end-to-end, now from the first paint. | 9.7 |
| 2 | Typographic system | Census: **8 distinct px sizes in style.css (13/14/16/18/20/26/30/34), zero sub-9.5px, zero font-size literals in app.js**; mono tokens carry 82 uses (sm 40 / xs 30 / md 12). landing.css adds 5 sizes (14–30px), all ≥14, kickers reuse `--mono-xs/--mono-sm` — no new micro-type. Saira 600/700/800 all loaded (no faux-bold). `tabular-nums` on every numeric surface incl. scored-nums. | 9.7 |
| 3 | Color system | Re-measured: accent 5.13:1 bg / 4.76:1 panel; rink-head 6.15:1 panel; sponsor gold `#b8925e` 6.37:1 on inset; gate CTA white-on-`#1f6cb8` 5.39:1; faint 5.74:1 panel. Sole sub-AA text in the product remains `.pill-count` 10.5px at opacity .7 ≈ 3.4:1 — decorative count suffix, last of its tier. Both new surfaces (gate, sponsor) are AA-clean. | 9.7 |
| 4 | Layout & hierarchy | Tonight-strip 2×2 verified effective (85px→156px cells) — kills the R7 tight-grid nit and restores KPI breathing room. Mixed-slate by-rink grouping + truncation-proof scored-chip intact. Sponsor strip sits as a quiet footer band below the marquee — correct long-tail placement. Gate adds a brand→app hierarchy layer without disturbing dashboard rhythm. Single-rink slate renders one clean Upcoming card, cols-head hidden <900px. | 9.7 |
| 5 | Density & data presentation | **The anchor, unchanged at 9.4.** Fix 1 shipped at byte level but the sticky `th` still cannot engage (scroll-container trace in the fix table above) — long mobile tables still lose their header. NEW: the fade `::after` collides with and kills the "swipe ›" chip at ≤699px while painting invisibly (panel-on-panel) — an affordance regression on exactly the tables that still x-scroll. Standing craft otherwise intact and re-verified: coarse sticky-left first column, zebra, two-layer right fade, 44px cell min-widths, print discipline, skeleton termination contract now 40s on League/Analytics. | 9.4 |
| 6 | Interaction & micro-feedback | Every path still terminates: api() 30s AbortController + retry-once (L882–894), 8s nudge + 40s deadline + debounced retry on League/Analytics (L1433–1457). Two findable defects keep this off 9.7: (a) **Team tab's inline watchdog was never migrated** — still 8s/20s hard error (L1985–1994) against a 25s abort (L1999), so "Timed out after 20s" can print while the request is in flight; the R7-4 race was fixed in the shared helper but not at the flagged site (the recurring copy-to-next-site pattern). (b) The gate is `role=dialog aria-modal` with no Escape handling or focus trap — keyboard users have exactly one exit (Enter the Dashboard). | 9.6 |
| 7 | Motion design | Inventory remains functional-only: LIVE pulse, score-flash, capped entrance, PTR spinner, gate fade (opacity+visibility 0.45s). RM coverage now spans **both files** — style.css blocks L1206/1286/1392 + landing.css L172–174 (gate, CTA, sponsor, all transitions killed). Count-ups gated; hero watermark static by design (L1597). Nothing decorative; palette-safe. | 9.7 |
| 8 | Depth & material | Luminance ladder intact (`#070b12` → panel → panel-2 → inset wells); gate card = solid panel + hairline + one soft shadow, zero backdrop-filter; sponsor strips use `--inset`; tonight-kpi gradient subtle; landing radial glows whisper-alpha and theme-aware. No blur soup, no fake shadows, both new surfaces inherit the material system. | 9.7 |
| 9 | Consistency & component quality | One scorecell anatomy verified end-to-end (emit L1202 + CSS L1692–1703); `.scrim` CSS present; error/empty states uniform. New drift from the two new surfaces: gate CTA introduces a **fourth blue** (`#1f6cb8`, untokened — `--union` #2a7fd4 was already AA-large at 4.04:1 for the 19px bold label) and sponsor gold `#b8925e` appears as a 3× literal; `.sponsor-strip` styles live in landing.css; landing.css header comment says "localStorage" while the code uses sessionStorage; inline `margin-top` emissions grew 15→17 (the shared watchdog's own retry button emits one); 2 raw `${data.error}` sites remain (L2306/L2317 league paths — esc() now covers 9 of 12 error sites, R7-5 class nearly closed). Each small; together they hold this at 9.5. | 9.5 |
| 10 | Mobile ergonomics | All three R7 clusters closed and verified (fix table). Tap-floor census at 390px clears: pills 44 (L2093), pal-item ≈46, kpalBtn 44 (L1956/2095), **button.small 44 (L2114 — every retry/CTA)**, nav 48 coarse, gate CTA 52, gate/strip sponsor rows ≈46–54; release-gate grep clean (only `min-width:0` text-guards + the ≥900px desktop block). 16px inputs (no iOS zoom), safe-area insets, PTR. No new sub-44 control introduced by the gate or strip. Residual: data-table rows ≈40px — a table-craft nit anchored under density, not a control. | 9.7 |

**WEIGHTED TOTAL: 9.6**/10
(identity 9.7×.12 + type 9.7×.12 + color 9.7×.10 + layout 9.7×.12 + density 9.4×.12 + interaction 9.6×.10 + motion 9.7×.08 + depth 9.7×.08 + consistency 9.5×.08 + mobile 9.7×.08 = **9.638** → 9.6)

Gate check: release demands ≥9.7 per dimension — dims 5 (9.4), 6 (9.6), 9 (9.5) remain short. Total held at 9.6 (9.612→9.638): the density fix shipped but its mechanism didn't deliver, and new-surface one-offs offset the layout/mobile gains. All movement this round is byte-verified, not claimed.

## The 3 cheapest fixes toward 9.7-per-dimension

1. **Give the TABLE the vertical scroll context, and un-kill the swipe chip (density 9.4→9.7, +0.036 weighted — biggest delta left).** The card was never the scrollport — the table (display:block + overflow-x:auto ⇒ computed overflow-y:auto) is. Two edits in the existing `@media(max-width:699px)` blocks, no markup change: (a) add `.card:has(table) table{max-height:65vh}` — the table already has overflow-y:auto computed, so a height cap turns it into a real vertical scrollport and the existing `th{position:sticky;top:0}` engages (coarse sticky-left keeps working in the same scrollport); (b) **delete the appended `.card:has(table)::after` fade (L2103–2108)** — it paints panel-on-panel (invisible) and its cascade position kills the "swipe ›" chip. Optionally keep a card-level `max-height:70vh` for non-table cards only. Est. delta +0.036.
2. **Finish the watchdog/escaping migration at the flagged sites (interaction 9.6→9.7 + consistency credit, ≈+0.018).** (a) Replace Team's inline 8s/20s watchdog (app.js L1985–1994) with the shared `armSkeletonWatchdog($content, 'team data', …)` — the 40s deadline already exceeds the 25s abort+retry, killing the last "timed-out prints while in flight" race; (b) wrap the 2 remaining raw `${data.error}` interpolations (L2306, L2317) in esc(). Both are two-line edits at sites already named. Est. delta +0.010 (interaction) +0.008 (consistency).
3. **Tokenize the landing/gate one-offs (consistency 9.5→9.7 path, +0.008–0.016).** Add `--union-cta:#1f6cb8` + `--sponsor-gold:#b8925e` (or map the CTA to existing `--union` — 4.04:1 is AA-large for the 19px bold label), hoist `.sponsor-strip` styles from landing.css into style.css, fix the localStorage/sessionStorage comment drift, and swap the 17 inline `margin-top` emissions for `.mt-8/.mt-16` utilities (data-driven inline styles stay). Est. delta +0.008–0.016.

Post-fix estimate: 9.638 + 0.036 + 0.018 + 0.008 = **9.700 → 9.7 weighted**, with every dimension at the 9.7 gate. Beyond that, the residual is the preseason data ceiling and the 5.6 MB `/api/players` payload (27.5s measured) — a server-side gzip/paginate job, outside UI scope.

VERDICT: 9.6/10
