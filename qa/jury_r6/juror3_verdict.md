# CAHL UI/UX JURY — R6, Juror 3/5 — VISUAL CRAFT lens (v59)

- Method: Chrome-free static audit of deployed bytes — `/static/css/style.css?v=59` (85,364 B), `/static/js/app.js?v=59`, `/` (index), fetched 2026-09-04; `/api/version` = `{"js":59}` confirms v59 live; index refs `js?v=59 css?v=59` (sync clean).
- v55→v59 delta (unified diff vs saved `qa/jury_r5/style_v55.css`): exactly 3 CSS hunks — **(a)** autoToggle `:has(#autoToggle:checked)` checked-state feedback (R5 convergent fix #1 — LANDED), **(b)** `.pal-input-row input` 15px→16px iOS-zoom fix (R5 convergent fix #2 — LANDED), **(c)** NEW R6 enrichment block: `.tonight-strip` night KPIs, `.scored-strip` scoreboard chips, `.rink-head` rink grouping headers. JS diff: enrichment renderers + v56–59 API-abort/retry/renderTeam-guard work (out of craft scope).
- Gate changed: every dimension must score ≥ 9.7. **The v57 Today-page enrichment introduced NEW craft violations** (sub-9px mono, scale drift, a new sub-AA hue) on top of the standing ledger.
- Score lineage: 7.8 (R2) → 8.1 (R3) → 8.5 (R4) → 8.7 (R5) → **8.7 (R6)** — first non-advancing round (8.658 vs 8.660): the two landed fixes were offset, point for point, by enrichment debt.

## Dimension scores

| # | Dimension | Evidence (v59 bytes) | Score |
|---|---|---|---|
| 1 | Visual identity | Rink-lines watermark block byte-unchanged (verified L~1586–1634 region + watermark rules intact in v59); team-watermark Saira 800 + broadcast-bug brand stack still read as rink-broadcast product. New rink grouping headers actually *reinforce* rink identity — but their sub-AA hue (dim 3) keeps the polish from showing. | 8.9 |
| 2 | Typographic system | Census grew **28 → 29 distinct px sizes, still zero scale tokens**. The enrichment block added `27px` (tonight-num) and — worse — **`8.5px` (.tonight-label) and `7.5px` (mobile .tonight-label)**, a new low for a label that 9.5px mono labels already render fine. Standing stragglers all persist byte-identical (5th round): `.heat-val` 12px, `.heat-name`/`.gd-empty` 13px, `.sb-name` 19px base + 20px @media, `.t-home/.t-away` 16.5px + 17px split, `header h1` 17px/16px vs `.brand-text` 22px, `.tl-game.otl` 8px. `--mono-md` still dead (0 uses; xs=2, sm=3). 37 mono rules carry inline px sizes; the overlay-not-edit pattern is now in its 5th round. | 7.6 |
| 3 | Color system | Accent discipline holds (Goal Red only in live/lead/interactive contexts; CBJ hues structural). WCAG recomputed on real v59 tokens: dark `--accent` #e8253c = **4.47:1 on bg / 4.18:1 on panel — sub-AA for 9.5px red mono labels, 4th round carrying**; `#ef3d54` = 5.13:1/4.80:1, one-token fix. **NEW v59 violation:** `.rink-head` (10px mono, rink grouping headers) uses `var(--union)` = dark #2a7fd4 = **4.46:1 on panel — sub-AA on a brand-new class, shipped in the enrichment block**. Also `button:hover #3a8fe0` off-token (2nd round). Light theme: accent 5.63:1 PASS, muted 5.13:1 PASS, **faint #8a99ad = 2.90:1 FAIL** (4th round). Faint/muted/text-2/tonight-labels otherwise healthy (muted 5.97:1, text-2 11.47:1). | 8.4 |
| 4 | Layout & hierarchy | Enrichment improves marquee logic: tonight-strip gives night-level KPIs a proper 4-col grid above the game list; rink-heads add a real grouping rhythm; scored-strip uses sane `auto-fill minmax(170px)` chips. Marquee-vs-longtail span (34px cmp-h2h → 7.5px labels) is now 4.5× — impressive top, incoherent bottom. 11-track cols-head calc() system intact. | 8.9 |
| 5 | Density & data presentation | Sticky theads, zebra (color-mix panel-2 40%), 20 `tabular-nums` occurrences (+tonight-num/scored-nums correctly set), stable grids, no jiggle; enrichment chips ellipsize (`scored-teams` nowrap+ellipsis). Scored-chip numerals use display face at 20px — proper marquee treatment. | 8.8 |
| 6 | Interaction & micro-feedback | v56 `api()` AbortController + retry and autoToggle checked-state feedback landed (dots+border+panel-3 via `:has()`), but craft-scope defect persists: `tr.link:focus-visible` has the outline rule yet **no background parity with `tbody tr:hover`** (L491 hover, no sibling focus rule) — keyboard rows are invisible locators. Enrichment KPIs/chips are static panels (no hover/press states) — acceptable, not 9.7. | 8.9 |
| 7 | Motion design | All 11 keyframes intact and purposeful; live-dot/breathe/flash/rise-in unchanged; three reduced-motion blocks still kill every animation; enrichment added zero new animation — restrained, exactly right. | 9.0 |
| 8 | Depth & material | Still the best dimension: 1 backdrop-filter in the whole sheet, tokenized shadow families, tint-stacked tonight-KPI gradient (`panel-2→panel`) is a clean luminance step, hairline borders on all new chips, no blur soup, no fake shadows. | 9.1 |
| 9 | Consistency & component quality | Scored-chips/tonight-KPIs follow the radius/dim-bg/1px-border anatomy — good. But the type-scale scatter now leaks into brand-new components on day one (tonight-label 8.5/7.5px instead of the existing mono tokens), and `.rink-head` picks union instead of the muted used by every other mono group label — two new one-off systems in a single block. Token uses remain tokens-in-name-only (xs=2, sm=3, md=0). | 8.3 |
| 10 | Mobile ergonomics | 44px hit floors + tap-highlight parity hold (v55 fix intact); enrichment has a real ≤599px compaction pass (22px/15px nums, reduced padding). But `.tonight-strip` stays **4 columns at 390px** (no 2-col stack — `repeat(4,1fr)` with only gap/padding adjustments), and tonight-label drops to **7.5px** — below every legibility floor the rest of the sheet respects. Palette input 16px iOS-zoom fix verified landed. | 8.9 |

**WEIGHTED TOTAL: 8.7/10** (identity 1.068 + type 0.912 + color 0.840 + layout 1.068 + density 1.056 + interaction 0.890 + motion 0.720 + depth 0.728 + consistency 0.664 + mobile 0.712 = 8.658)

## The 3 cheapest fixes that would raise the score most

1. **Retire the mono overlay: edit original rule bodies and normalize the enrichment's new labels** (~1h): map the 29 hard px sizes onto 3 real tokens — kill `--mono-md`'s dead status by actually using it; fix the named stragglers (heat-val 12→11, heat-name/gd-empty 13→12.5, sb-name 19/20→token, t-home/away 16.5→17→token, tl-game.otl 8→9, header h1→brand token); specifically set `.tonight-label` 8.5px/7.5px→`var(--mono-xs)` (9.5px) — nothing at 8px or below survives review. **Delta: +0.27** (type 7.6→8.6, consistency 8.3→8.9). Fifth round being told: edit originals, don't append.
2. **One contrast pass, three one-token swaps** (~10min): dark `--accent` #e8253c→`#ef3d54` (5.13:1 bg / 4.80:1 panel, hue-identical); `.rink-head` dark color→`#4a9ae6` (6.19:1 panel, same blue family — or reuse muted like every other group label); light `--faint` #8a99ad→`#76879c` (3.67:1, still clearly subordinate to muted 5.13:1); delete `button:hover #3a8fe0` → `var(--union)`. **Delta: +0.11** (color 8.4→9.1; also un-bleeds identity/consistency at the margin).
3. **Tonight-strip mobile stack + row keyboard parity** (~30min): at ≤599px set `.tonight-strip { grid-template-columns: repeat(2, 1fr) }` (4×~90px cells at 390px is thumb-hostile and drives the 7.5px label); add `tbody tr:focus-visible, tr.link:focus-visible { background: var(--panel-2) }` matching hover. **Delta: +0.05** (mobile 8.9→9.3, interaction 8.9→9.1).

Combined realistic ceiling for R7 if all three land: **~8.9–9.0/10**. The ≥9.7-per-dimension gate remains blocked by the type-scale debt (largest weighted deficit) and now by enrichment components that shipped with their own one-off type/contrast choices instead of inheriting the system.

VERDICT: 8.7/10
