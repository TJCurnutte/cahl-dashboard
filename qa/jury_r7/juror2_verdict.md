# UI/UX JURY — R7, Juror 2/5 — MOBILE LENS (390×844), verify-and-score

Site: https://cahl.neural-forge.io · Deployed build verified fresh: `/api/version` → `{"js":61}`, markup loads `style.css?v=61` + `app.js?v=61`, inline `APP_VERSION = 61`. Judged from deployed v61 bytes fetched this round (style.css 86,371 B / 2,091 lines sha256 b0b040…9586; app.js 153,328 B / 3,091 lines sha256 ca720b…f47; byte-identical to the sibling juror's saved copy), plus live `/api/today`. Chrome-free per standing directive; 390px lens traced through media queries and shipped rules. Lineage: 7.7 → 8.6 → 8.7 → 9.5 (R6, best of panel) → this round.

**Data context (honesty rule):** preseason thin — 2 upcoming games (Brewskies–Trash Pandas 7:40, Whalers–Stepbros 8:50, both Chiller Dublin), `played:null`, no finals, no scores tonight. Penalized only where design could compensate (empty states, skeleton termination, tonight-strip); scored-strip renders empty on this slate by design.

## R6 fix verification (my three cited defects, byte-checked against deployed v61)

| R6 cited fix | Verdict in deployed v61 |
|---|---|
| (1) Coarse-pointer 44px floors for `.pill` / `.pal-item` / `#kpalBtn` | **SHIPPED — verified.** Append-only block `R6-2 — COARSE-POINTER TOUCH FLOORS` at style.css L2084–2091 inside `@media (hover: none), (pointer: coarse)`: `.pill{min-height:44px}` (L2088), `.pal-item{padding:14px …}` → ≈46px row (L2089), `#kpalBtn{min-width:44px}` (L2090). Effectiveness checked: every `.pill` emit (app.js L191/197/236/239/245) sits in `.picker-days`/`.picker-leagues`/`.pill-row` flex containers → blockified → min-height applies. Combined with standing floors (header cluster 44px, nav 48px coarse L1159, th/td 14px pad, `tl-game` −11px hit extension, `cmp-picker` buttons 48px, `cal-cell.has-games` 48px, typeahead 13px pad), the R6 sub-44 clusters are closed. |
| (2) By-Rink mixed-slate finals recovery | **SHIPPED — verified.** app.js L1188: `upcoming.concat(finals).forEach(g => { … })` with an honest comment block (L1180–1186) naming the R6-1/R6-2 convergent find. The self-cancelling ternary is gone; mixed Final+Upcoming slates keep rink grouping, and each row still carries its own status chip. |
| (3) Scored-chip two-line stacked anatomy | **SHIPPED — verified.** app.js L1178 emits 4 spans (home, home_score, away, away_score); style.css L1677–1699 defines a `grid-template-columns: minmax(0,1fr) auto` chip — home line + right-aligned 20px accent numeral, away line + 16px muted numeral, `--inset` well + hairline. No truncation possible at 320px; the retained `text-overflow` is a last-resort guard, not the mechanism. `nth-of-type(3)/(4)` dimming works because all four children are spans. |

**Bonus sweep — the v61 pass also landed the other jurors' convergent carries (all byte-verified):** `animateNumbers` respects `prefers-reduced-motion` (app.js L773/779 — R6-4's 3-round carry); shared `armSkeletonWatchdog` 8s nudge / 20s error+retry now covers League (L1419) and Analytics (L2387) in addition to Team; `.scrim` rows finally have CSS (L436–438 — R6-1's dead-class find); cols-head double-paint deleted (L2044–2050, real spans only); `#3a8fe0` hover retokened to `--union-hover` (zero raw hits remain); `tonight-label` 7.5px → `--mono-xs` 9.5px (R6-3's convergent find); mono scale retokened (`--mono-md` now a live canonical 12px).

## Scores (fresh, at 390px)

| # | Dimension | Evidence at 390px | Score |
|---|---|---|---|
| 1 | Visual identity | Unchanged from v59 and audited clean: rink-lines hero watermark (blue/red lines + 130px center ring <480px, light-theme variants), 2-stop union→accent header stripe, live-pill grammar, CBJ-only hues (hex census: zero off-token literals; the sole previously flagged `#3a8fe0` is now the `--union-hover` token). Saira Condensed numerals 30→34px on the scoreboard. Distinctly hockey, never generic admin. | 9.6 |
| 2 | Typographic system | **Sub-9.5px type is now extinct** — grep for `font-size` below 9.5px returns zero matches across all 2,091 lines (R6's 7.5/8.5px `tonight-label` straggler now `--mono-xs`). Mono scale is 3 live tokens (9.5/10.5/12px), `--mono-md` retokened from dead 11.5px. Saira 600/700/800 all present in the fonts href (no faux-bold). `tabular-nums` on every numeric surface including the new `.scored-nums`. Residual: `tonight-num` stays 22px in 85px-wide KPI cells at 390px — scale-heavy but truncation-free. | 9.6 |
| 3 | Color system | `--accent #ef3d54` measured 5.13:1 on `#070b12`, 4.76:1 on panel (my own WCAG computation); `--muted/--faint` AA block re-declares at cascade end (L1867–1874) with computed worst-case ratios documented in the comment — dark 5.10:1 / 4.9:1, light 4.91:1 / 4.8:1. New scored-chip uses tokens throughout. Only sub-AA text found: `.pill-count` 10.5px at opacity .7 ≈ 3.4:1 on panel-2 — a decorative count suffix, the last of its tier. | 9.7 |
| 4 | Layout & hierarchy | Both R6 layout defects fixed and verified: mixed-slate by-rink grouping restored (concat(finals)) and scored-chip can no longer truncate. Cols-head double-paint deleted so desktop header labels paint once. Residual at 390px: `tonight-strip` holds 4-up (≈85px cells, 22px numerals, 16px condensed time) — fits without truncation but is the tightest grid on the page and forfeits KPI marquee scale; my R6 evidence line claimed a 2×2 ≤599px fallback that **does not exist in v59 or v61 bytes** (my write-up error, owned here; the shipped ≤599 block only tightens gap/type). Single-rink slate correctly renders one "Upcoming" card with the 11-track cols-head hidden <900px. | 9.6 |
| 5 | Density & data presentation | Standing craft intact and re-verified: block-tables <900px with 44px min-widths, coarse-pointer first-column sticky-LEFT (works — engages against the table's x-scroll), two-layer self-hiding fade + "swipe ›" chip, zebra via color-mix, tabular numerals, print discipline. `armSkeletonWatchdog` extends the 8s/20s termination contract to League/Analytics — a skeleton can never be final state on any section. Persisting nitpick: `th{position:sticky;top:0}` is inert inside the block-table (overflow-x:auto forces overflow-y to auto with content-height, so the top never engages) — the header scrolls away on long mobile tables. | 9.4 |
| 6 | Interaction & micro-feedback | Every path terminates with feedback: `api()` 30s AbortController ceiling + one silent retry + stale-render-token guard (L856–885); 8s "Still loading…" nudge / 20s error+retry on Team, League, Analytics; PTR with progressive spinner hint; btn-press scale; nav :active + transparent tap-highlight; toast confirms refresh. Could not construct a hung or silent state from the source at any tab. | 9.7 |
| 7 | Motion design | Last motion gap closed: count-ups now gated on `prefers-reduced-motion` (paint final value, app.js L779). Four reduced-motion blocks + global 0.01ms transition kill verified; inventory remains functional-only (LIVE pulse, score-flash, capped-stagger entrance, hero-glow breathe, PTR spinner, heat-bar width). Nothing decorative; palette-safe. | 9.7 |
| 8 | Depth & material | Luminance ladder intact (`#070b12` bg → `#0d1522` panel → `#131b2b` panel-2 → `#e9eef6`-light equivalents), `--inset #090d16` wells used consistently by the new chip, hairlines at 8–26% alpha, `--blur:none` with only the functional palette overlay at 3px, minimal shadows. `tonight-kpi` panel-2→panel gradient is subtle, in-system elevation. No blur soup, no fake shadows. | 9.7 |
| 9 | Consistency & component quality | One scorecell anatomy now genuinely everywhere — the scored-chip inherits the sb-card construction (display-face, tabular numerals, inset well, muted away line); `.scrim` has CSS; header sort affordances uniform; empty states are cards with retry, never bare text. Residual: 45 inline `style=` emissions in app.js — ~15 static `margin-top` hacks (leaders h3s, retry buttons, picker-hints) that belong in classes; the ppg-bar `width/left` inline styles are data-driven and legitimate. `.pill-count` opacity is the only style-token drift. | 9.6 |
| 10 | Mobile ergonomics | R6's named sub-44 clusters closed and verified (see fix table). Tap-floor census at 390px now clears: pills ≈44px, palette items ≈46px, kpalBtn 44px, nav 48px, table cells ≈47px, calendar 48px, typeahead ≈45px, header cluster 44px with padding-only ≤430 squeeze (release-gate grep: zero `min-height/width:0` inside any max-width media block — the L1103 hits are the ≥600px desktop sidebar). iOS zoom-safe 16px palette input, safe-area insets, PTR. Remaining findable defect: `button.small` (6px pad, 12px text) renders ≈33px tall — every "Retry"/"Pick My Team"/"Load leaderboard" button. | 9.6 |

**WEIGHTED TOTAL: 9.6**/10
(identity 9.6×.12 + type 9.6×.12 + color 9.7×.10 + layout 9.6×.12 + density 9.4×.12 + interaction 9.7×.10 + motion 9.7×.08 + depth 9.7×.08 + consistency 9.6×.08 + mobile 9.6×.08 = 9.612 → 9.6)

Gate check: the release gate demands ≥9.7 per dimension — six dimensions sit at 9.6–9.7 and density (9.4) is the anchor. Still short of the gate, but the trajectory is monotonic and this round's gains are all byte-verified, not claimed.

## The 3 cheapest fixes toward 9.7-per-dimension

1. **Give mobile tables a real sticky header (density 9.4→9.6, +0.024 weighted — biggest single delta left).** Two lines at <699px: `.card:has(table){max-height:70vh; overflow-y:auto}` (+ a bottom fade hint). This makes the card the vertical scroll context, so the existing `th{position:sticky;top:0}` actually engages, and the coarse-pointer first-column sticky-left keeps working inside the table's own x-scroll. No markup change. Est. delta +0.024.
2. **`tonight-strip` 2×2 at ≤430px (layout 9.6→9.7).** `@media (max-width:430px){ .tonight-strip{grid-template-columns:repeat(2,1fr)} }` — KPI cells go 85px→≈178px, restoring 26px marquee-scale numerals and killing the tightest grid on the page. This was wrongly credited in my R6 evidence; shipping it makes the record true. Est. delta +0.012.
3. **Coarse-pointer floor for `button.small` (mobile 9.6→9.7).** Extend the existing R6-2 block: `.small{min-height:44px; padding:10px 14px}` inside `@media (hover:none),(pointer:coarse)` — covers every retry/CTA across error and empty states with the same append-only pattern. Est. delta +0.008.

(Honorable mention: replace the ~15 static inline `margin-top` emissions with two utility classes — consistency 9.6→9.7, +0.008; data-driven inline styles should stay.)

Post-fix estimate: 9.612 + 0.044 = 9.656 → **9.7 weighted**. The residual beyond that is the preseason data ceiling (2-game slates, zero-value leaders) which design has already absorbed via watchdogs, em-dash placeholders, and empty-state cards.

VERDICT: 9.6/10
