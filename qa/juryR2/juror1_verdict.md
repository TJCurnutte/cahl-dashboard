# CAHL UI/UX JURY — R2, Juror 1/5 (v51, js=51 confirmed via /api/version)

**Method note (per operator directive):** browser automation was cut off mid-probe by the owner (remote debugging disabled). Before the cutoff I verified live at 1440x900: first-run League tab (defect #2), first-run Today hero, UPDATED 13:17 stamp, nav anatomy. Everything else is scored from deployed source (/, /static/css/style.css?v=51 [1959 lines], /static/js/app.js?v=51 [2909 lines]) + API responses (/api/today, /api/teams, /api/league/<id>, /api/team/<id>×2). Dimensions that need a rendered screen (perceived perf, motion feel, true mobile layout) are marked **(static)**. Live-probe evidence is marked **(live)**.

## Round-1 critical defect re-verification

| # | Defect | Verdict | Evidence |
|---|--------|---------|----------|
| 1 | Team tab hang on stuck skeleton | **FIXED** | `loadTeamContent`: skeleton → 8s "Still loading your team…" nudge → 20s `teamErrorHtml` hard error with Retry button; 25s AbortController; `finally` clears both timers and renders error card on `hardFail`. Skeleton cannot be terminal state. API confirmed: `/api/team/<id>` 200 in 2.7s/3.3s for both a data team (1st Ohio Battery) and a no-data team (Yetis, 0 games). (static + API) |
| 2 | League dead for first-run visitor | **FIXED — verified live pre-cutoff** | `renderLeague`: `if (state.leagueId) … else if (state.leagues.length)` auto-selects first league via `chooseLeague` (persists + loads). Live at 1440x900 with localStorage wiped: League tab rendered Sunday B East, day+division pills with counts, "SUNDAY B EAST FALL 2026" context, 3 upcoming game cards, 0 skeletons. No dead panel. **(live)** |
| 3 | Hero name clipped at x<0 | **FIXED** | R1-B: hero-top is normal flex flow (no absolute); `.hero-name-wrap { max-width:100%; min-width:0 }`; `.hero-glow` forced `position: static !important` (kills the old -14%/inset:-35% glow shell); `.hero-glow .hero-team { overflow-wrap:anywhere; white-space:normal }`. `.hero-card` still has `overflow:hidden`, but the name now lives in flow and wraps. (static) |
| 4 | NO GAMES LIVE must be badge, not toggle | **PARTIALLY FIXED — vocabulary yes, toggle no** | Label is now a status vocabulary: `syncLivePolling()` sets `LIVE` / `NO GAMES LIVE`, `body.has-live` drives dot pulse. **BUT** shipped HTML is still `<label id="livePill" title="Live status — click to toggle 30s auto-refresh">` containing a hidden `#autoToggle` checkbox with an active `change` handler that starts a 30s `POST /api/refresh` loop, plus `cursor:pointer`. It is still a control wearing a badge's clothes, and clicking it produces zero visible feedback. Dead `.switch` CSS remnant also still present. |
| 5 | Analytics heat/gd broken | **FIXED** | Heat rows are three separated elements: `.heat-name` (flex:1, ellipsis) + `.heat-track` (40% bar, data-w grown via double-rAF) + `.heat-val` (mono tabular, right-aligned); no more fused "Nomads0". GOAL DIFF is a real dot-axis viz with `.500` dashed line + labels-below-row, **and** an honest preseason state: API shows all GF/GA = 0 → client renders `gd-empty` "No goal data yet — season hasn't started" instead of a lying axis. Bye-Week rows filtered from all standings-derived visuals. (static + API) |

## 10-dimension scores

| Dimension | Evidence | Score |
|---|---|---|
| 1. Visual identity | Dark "rink night" canvas, CBJ Union Blue #2a7fd4 + Goal Red #e8253c tokens, Chiller logo + CAHL wordmark, broadcast vocabulary (LEAGUE / SCORES, TONIGHT'S PACEMAKERS, day pills with counts) — reads as a sports broadcast product, not admin. Defects: identity is dark-only (light theme is a generic light skin), header chrome is sparse at 1440, idle live-pill reads as a gray utility chip. | 8.0 |
| 2. Typographic system | Saira Condensed display on every numeral surface (.sb-num/.t-score/.score/.pts grouped selector), JetBrains Mono for eyebrows/headers/th (54 usages), tabular-nums on 18 rule sets, --mono-xs/sm/md tokens exist. Defects: mono scale not actually consolidated — 26 hardcoded 9.5/10.5/11.5px sizes vs 5 token usages; 9.5px micro-labels strain legibility. **(static)** | 8.2 |
| 3. Color system | One accent discipline (red=live/loss/danger; union=interactive), tint-stacked surfaces, theme-swapped token blocks. Defects (computed WCAG): `--faint` #5a6a82 on panel = 3.35:1 dark / #8a99ad on #f2f5fa = 2.65:1 light — and the idle NO GAMES LIVE label uses `color: var(--faint)` at 9.5px = sub-AA status text in both themes; dark `--accent` on panel 4.18:1 (large-only) at 9.5px mono; light win #0e8a56 4.39:1. | 7.6 |
| 4. Layout & hierarchy | Clear marquee (hero) → long-tail (rows) separation, eyebrow mono section headers, day→division→sub-tab scanning rhythm, today-row grid-areas. Defects: desktop 11-track cols-head positioned by magic-number calc()s (left: calc(366px + (100% - 696px)/2)…) — brittle to any metric change; large dead whitespace under short tab content at 900px-tall viewports (seen live on League). **(live+static)** | 7.8 |
| 5. Density & data presentation | Sticky th top+left, zebra via color-mix on standings/leaderboard/roster, sortable leaderboard AND standings (`th[data-sort]` with ↑/↓ arrows, Enter/Space operable), tabular-nums everywhere, Sports-Reference table discipline. Defects: below 900px tables go `display:block; overflow-x:auto` scroll (see mobile). **(static)** | 8.0 |
| 6. Interaction & micro-feedback | Hover on every pill/row, focus-visible rings, sb-card press, refresh disables+spins, announce() SR toasts, api() network-error copy, typeahead 180ms debounce with stale-message clear. Defects: live-pill click toggles hidden auto-refresh with **no visible response** (label driven by game state only) — a tap that answers with nothing; duplicate loadActiveTab skeleton-then-render double-flash. **(static)** | 8.0 |
| 7. Motion design | Purposeful set: live-dot-pulse 1.4s, score-flash, rise-in stagger capped at 5th child, shimmer skeletons, pal-fade/pop; reduced-motion block kills all animations + transitions (0.01ms) including hero-glow. Defect: hero-glow-breathe is an infinite ambient glow = decorative motion the brief forbids (subtle, but it's there). **(static)** | 8.5 |
| 8. Depth & material | Luminance-stacked 6-surface scale (bg/bg-raise/panel/panel-2/panel-3/inset), hairline borders 8–26% alpha, dark shadows deliberately suppressed, real soft shadows only in light theme, single 3px modal scrim blur. No blur soup, no fake shadows. Cleanest dimension. **(static)** | 8.6 |
| 9. Consistency & component quality | One .pill system for day/league/level, unified status-chip/status-badge grammar, one tabular-nums scorecell group, a11yFix scope pass, aria-live regions. Defects: two conflicting .live-pill definitions shipped (32px base + 3px-padding redefinition = anatomy drift), dead `.switch` rule, 4 competing .fade-in overrides (two `animation:none !important`), 26 off-token mono sizes. **(static)** | 7.8 |
| 10. Mobile ergonomics | Bottom nav dock w/ 44px+ targets (48px coarse), today rows stack to single-column cards <480px, header compressed <430px, safe-area aware data-note, footer out of nav. Defects: header controls collapse below target — kpal 26px, theme/refresh `min-height:0; padding:4px` ≈ 26–28px, live-pill 26px, all < 44px; standings/roster/leaderboard have **no card fallback** (block-scroll at 900px min-width); idle pill contrast 2.65:1 in light; viewport blocks user scaling (`maximum-scale=1.0, user-scalable=no`) — accessibility fail. **(static)** | 6.8 |

## WEIGHTED TOTAL

identity 8.0×.12 = 0.960 · type 8.2×.12 = 0.984 · color 7.6×.10 = 0.760 · layout 7.8×.12 = 0.936 · density 8.0×.12 = 0.960 · interaction 8.0×.10 = 0.800 · motion 8.5×.08 = 0.680 · depth 8.6×.08 = 0.688 · consistency 7.8×.08 = 0.624 · mobile 6.8×.08 = 0.544

**WEIGHTED TOTAL: 7.9/10**

## 3 cheapest high-delta fixes

1. **Finish the live-pill badge conversion** (~15 min): delete the hidden `#autoToggle` checkbox + its 30s `/api/refresh` change handler, drop `cursor:pointer` and the "click to toggle" title, remove the dead `.switch` rule. Fixes defect #4 for real, restores click-answers-with-feedback. Δ interaction 8.0→8.7, consistency 8.0→8.2 ≈ **+0.15 weighted**.
2. **AA the --faint token + pill label** (~10 min): raise --faint to ≥4.5:1 in both themes (dark ≈ #7e8ea6, light ≈ #64748c) — one token edit repairs 21 usages including the NO GAMES LIVE badge; optionally bump pill label to --mono-sm. Δ color 7.6→8.2 ≈ **+0.06 weighted** (and closes an a11y lawsuit-shaped hole).
3. **Card fallback for mobile tables + re-enable zoom** (~2–3 h): reuse the today-row stack pattern for standings/roster/leaderboard below 600px, and drop `maximum-scale=1.0; user-scalable=no` from the viewport meta. Δ mobile 6.8→7.8 ≈ **+0.08 weighted** and removes the worst mobile defect class.

**Note to panel:** my 7.9 is static-evidence-heavy by mandate. The five round-1 criticals are genuinely repaired (one partially — the pill is still a toggle); what holds v51 under 9 is not bugs but polish debt: off-token type sizes, sub-AA faint text, sub-44px header targets, scroll-soup tables on mobile, and residual chrome inconsistencies. Nothing here is a 9.7 yet.

VERDICT: 7.9/10
