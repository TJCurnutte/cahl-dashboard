# UI/UX JURY — R10, Juror 2/5 — MOBILE LENS (390×844), verify-and-score

Site: https://cahl.neural-forge.io · Deployed build verified fresh this round: `/api/version` → `{"js":70}`, shell loads `style.css?v=70` + `landing.css?v=70` + `app.js?v=70`, inline `APP_VERSION = 70`, `JS_VERSION = 70` (app.js L32) — version triple in sync. Judged from deployed v70 bytes fetched this round (style.css 88,845 B sha256 67fb387c…; app.js 157,005 B sha256 61a31053…; landing.css 8,892 B sha256 dba06448…; index 13,161 B), plus live `/api/today` (1.93 s, 5.2 KB), `/api/leaders` (1.26 s), and `/api/players` (4.81 s single-shot, 5,518,276 B, 5,862 rows). Chrome-free per standing directive; 390px lens traced through media queries and shipped rules. Lineage: 7.7 → 8.6 → 8.7 → 9.5 → 9.6 → 9.6 → 9.7 (R9) → this round.

**Data context (honesty rule):** preseason thin — 2 upcoming games (Brewskies–Trash Pandas 7:40 PM, Whalers–Stepbros 8:50 PM, both Chiller Dublin), no finals, 27 leagues. Penalized only where design could compensate; empty-state/skeleton termination contracts hold.

## R9 fix verification (my cited fixes, byte-checked against deployed v70)

| R9 cited fix | Verdict in deployed v70 |
|---|---|
| (1) De-blur the landing nav (`.lp-nav` backdrop-filter) | **SHIPPED.** landing.css L44–45 now `background: var(--bg); /* R9-4: nav blur removed — backdrop-filter budget stays with the app */` — zero `backdrop-filter` hits left in landing.css; blur census: style.css holds only the palette scrim 3px (L766) and the hero-glow's static decorative `filter: blur(10px)` (L1243, pre-existing, `pointer-events:none` background wash — not a material surface). Backdrop-filter count is back to 1, and it's the app's own scrim. |
| (2) Quantize the landing's parallel type scale onto the system scale | **SHIPPED.** landing.css literal sizes collapsed from 12 values (11/12.5/13.5/15/17/19/26/27/32/44…) to exactly **7: 13/14/16/20/26/30/34px — byte-identical to the dashboard's own style.css scale** (same 8 values incl. 18px, plus tokens). `.lp-f-icon` is now `font-family: var(--mono); font-size: var(--mono-sm)` (was 11px literal). The 11px outlier and both fractional body sizes are gone; landing consumes `--mono-xs`/`--mono-sm` tokens. The fork is closed — one type system, two files. |
| (3) `.lp-cta-sm` 44px floor | **SHIPPED.** landing.css: `.lp-cta-sm { min-height: 44px; … }`; the ≤430 block now adjusts only the primary `.gate-cta` (52px → 50px ≤430) — every CTA clears the floor at 390px. |
| Also directed: dead-link guard at the RIGHT sites | **SHIPPED — and live-validated end-to-end.** Conditional `p.player_id ?` guards present at all five render sites: leaderSection L282, League L1576/1579/1582, Analytics Top Scorers L2521 (the R9-5 "wrong sibling" class is closed at every site). Live `/api/leaders`: 5/5 rows in points, goals, assists **all carry player_id** — zero dead links shipping right now. |
| Also directed: dup `.rink-head` rule deleted | **SHIPPED.** The standalone `{ color: #4a9ae6 }` literal rule is gone; L1720 is now the light-theme override `html[data-theme="light"] .rink-head { color: var(--union-deep); }` — one rule per theme, AA comment retained. |
| Also directed: palette focus-within cue | **SHIPPED.** `.pal-input-row:focus-within { box-shadow: inset 0 0 0 2px var(--union); … }` — the 4-round carry is closed. |
| Not shipped: gate keyboard contract (my R9 fix #2) | **UNSHIPPED.** app.js L7–27: `gateDismiss()` writes sessionStorage, hides, removes, focuses `#main` — but the `role=dialog aria-modal="true"` gate still has **no Escape handler and zero `inert` refs** (grep: 0). Keyboard users still get one exit: Enter. Carried, 2nd round. |

**Standing ledger re-verified in v70 bytes:** api() 30s AbortController (30000 ×3) + silent retry; shared `armSkeletonWatchdog` ×4 consumers, 40000ms deadline ×3 (all above the 30s ceiling); esc() 153 uses, **zero raw `${data.error}`/`${e.message}` interpolations**; `concat(finals)` L~1214; `:has(#autoToggle:checked)` feedback ×2; count-up RM gate (`if (reduceMotion) return;`); tonight-strip 2×2 at ≤430 (L2118); coarse 44px floors (`#kpalBtn, .live-pill { min-height: 44px }`, `button.small { min-height: 44px }`); table-level scrollport (L2113–2114 `display:block; max-height:65vh; overflow-y:auto`) with base `th{position:sticky;top:0;z-index:5}` and sticky-left `td:first-child` engaging in it; "swipe ›" chip sole `::after` owner (L1931); zebra ×2; `tabular-nums`; inputs 16px (no iOS zoom); safe-area insets ×5 + `viewport-fit=cover`; Saira Condensed 600/700/800 + JetBrains Mono 400/500/600 + Inter hrefs; single sponsor mark (lockup.png only, index + app.js); paintStamp + 60s tick (L2919–2923); release-gate grep — every `min-h/w:0` hit inside max-width media is a flex text-guard (header h1, brand-block, controls, live-pill), zero tap-floor kills.

**Consistency residuals that persist (cited 3rd round):** `.sponsor-strip` rules still live in landing.css (11 rule hits; style.css 0) — app-wide component housed in the marketing file; landing.css L4 header comment still says `localStorage 'cahl-entered' skips` while the code uses sessionStorage; landing radius literals 7/8/10/14/16px against `--radius 6/9/12` (all six values still present, zero `border-radius: var(--radius-*)` uses in landing.css); 18 inline `margin-top` emissions in app.js (43 inline `style=` total).

**/api/players:** 4.81 s single-shot (v69/v70 persistence working warm) but **5.52 MB uncompressed body** — standing server-side carry. Fresh 5-burst this round: 11.8–33.8 s per shot, one shot exceeded my 45 s client timeout — the cold-instance class the Supabase layer targets; env not yet wired (creds pending from Travis per coordination log), so burst variance is real but the 30s abort + 40s watchdog still guarantee every skeleton terminates.

## Scores (fresh, at 390px)

| # | Dimension | Evidence at 390px | Score |
|---|---|---|---|
| 1 | Visual identity | Landing first touchpoint unchanged in its strengths: HD crest at `min(260px, 82%)` ≈ 260px on a 390px screen (≤719 block), rink-line margins, union/red radials, Saira Condensed 800 title, sponsor lockup in system gold, single crest/lockup mark family across gate + Today strip + landing. Dashboard shell token-clean. Hockey end-to-end. | 9.7 |
| 2 | Typographic system | **The parallel scale is dead.** landing.css now ships exactly the dashboard's 7 literal sizes (13/14/16/20/26/30/34) + mono tokens; 11px outlier gone; zero faux-bold (Saira 600/700/800 loaded); tabular-nums across tables; tonight-num 20px ≤599, tonight-label on `--mono-xs`. One type system, two files — nothing off-scale found at 390px with effort. | 9.7 |
| 3 | Color system | Recomputed both themes: accent #ef3d54 4.80 panel / 5.13 bg (light #ce1126 5.63 panel); sponsor-gold 6.42 dark / 6.18 light panel; faint 5.78/6.18, muted 5.97/6.39; CTA white-on-#1f6cb8 5.39; union-hover #2b71c4 4.93 under white text (tokenized, consumed only at button:hover — fill+white pair, AA); landing.css's sole raw hex is #ffffff on #1f6cb8 = 5.39. Sole sub-AA text in the product remains `.pill-count` (decorative count suffix, opacity .7). | 9.7 |
| 4 | Layout & hierarchy | Landing reads nav → hero → 4-feature grid → about → sponsor (stacks column + centers ≤719, lockup 170px) → footer CTA; links yield to CTA <719. Dashboard rhythm unchanged: tonight-strip 2×2 at 390, by-rink grouping, scored-chip two-line anatomy, sponsor strip quiet band. Single-rink slate renders one clean Upcoming card. | 9.7 |
| 5 | Density & data presentation | The R9-closed anchor holds in v70 bytes: table is its own scrollport (≤699, L2113–2114) so `th{sticky;top:0}` and sticky-left `td:first-child` both engage; swipe chip sole `::after` owner; zebra; 44px cell min-widths; headers persist over 65vh at 390px. With effort, nothing defect-grade remains at this viewport. | 9.7 |
| 6 | Interaction & micro-feedback | Every path terminates: api() 30s abort + retry, watchdog 40s + debounced retry on League/Team/Analytics, PTR, palette cue (now focus-within), autoToggle feedback, dead links gone (live-validated). One defect, found with effort and now 2 rounds old: the `role=dialog aria-modal` gate has **no Escape handling and no `inert`/aria-hidden on #app** (zero inert refs) — keyboard users get one exit: Enter. | 9.6 |
| 7 | Motion design | Inventory functional-only: LIVE pulse, score-flash, capped entrance, PTR spinner, gate fade, CTA arrow nudge, sponsor hover, hero-glow static. RM blocks: style L1215/1295/1401 + landing L218–220 (gate/CTA/arrow/sponsor/links/strip all `transition:none`); count-ups RM-gated (`if (reduceMotion) return;`). No decorative motion in either surface. | 9.7 |
| 8 | Depth & material | **Blur budget restored.** Backdrop-filter census: exactly one — the palette scrim (3px, L766). The landing nav de-blurred to `var(--bg)` + its existing hairline; surface ladder bg→panel→panel-2→panel-3→inset intact, no fake shadows, hero-glow's static blur is a `pointer-events:none` background wash, not a material. | 9.7 |
| 9 | Consistency & component quality | Tokens hold (union-cta/sponsor-gold consumed via var() both themes; no off-token hover hexes; rink-head single-rule-per-theme). But the system-discipline sweep on the landing is still partial after 2 citations: radius literals 7/8/10/14/16 vs `--radius 6/9/12` (zero token uses in landing.css), `.sponsor-strip` still housed in landing.css, L4 comment still says localStorage, 18 inline margin-top emissions. Each is one-mechanical-edit; together they're the last inch of the system. | 9.6 |
| 10 | Mobile ergonomics | Tap-floor census at 390px clears: pills 44, kpalBtn 44/min-width 44, `button.small` 44, gate CTA 52→50, lp-cta-sm 44, swipe chip decorative (exempt), lp-links hidden <719. 16px inputs (no iOS zoom), safe-area insets ×5 + viewport-fit, gate/strip focus-visible, release-gate grep clean, no truncation soup, sponsor card stacks clean at 390. No sub-44 control exists at this lens. | 9.7 |

**WEIGHTED TOTAL: 9.7**/10
(identity 9.7×.12 + type 9.7×.12 + color 9.7×.10 + layout 9.7×.12 + density 9.7×.12 + interaction 9.6×.10 + motion 9.7×.08 + depth 9.7×.08 + consistency 9.6×.08 + mobile 9.7×.08 = **9.682** → 9.7)

Gate check: the release bar is ≥9.7 per dimension. The raw total advances for the fourth straight round (9.662 → 9.682) and still rounds to 9.7, and the R9 type-scale fork — the biggest structural debt — is closed at the source. But **two dimensions sit at 9.6** (interaction, consistency), each with one named defect above; the interaction defect is now carried twice. All movement this round is byte-verified against deployed v70, and the dead-link class was additionally live-validated, not just grepped.

## The 3 cheapest fixes toward 9.7-per-dimension

1. **Give the gate its keyboard contract (interaction 9.6→9.7, ≈+0.010).** It declares `role=dialog aria-modal="true"` (index L51) but has no Escape handler and zero `inert` refs — make the declaration true: one `keydown` listener (Escape → `gateDismiss()`) plus `document.getElementById('app').inert = true` while the gate is shown and false inside `gateDismiss`. ~6 lines at the exact site (app.js L11–27), now a 2-round carry. Est. delta +0.010.
2. **Finish the landing system-alignment sweep (consistency 9.6→9.7, ≈+0.008).** (a) Replace the six radius literals (7/8/10/14/16px) with `var(--radius-sm/md/lg)` — the tokens exist, landing.css just never consumes them; (b) hoist the 11 `.sponsor-strip` rules from landing.css into style.css so the dashboard component lives with its system; (c) fix the one-word comment drift (localStorage→sessionStorage, landing.css L4). All mechanical, zero visual change. Est. delta +0.008.
3. **Convert the 18 inline `margin-top` emissions to `.mt-*` utilities (consistency reinforcement + type/layout hygiene, ≈+0.004).** Same census class as the hoist in #2: 43 inline `style=` emissions remain in app.js, 18 of them margin-top — a one-pass sed-to-utility conversion that makes the emission layer grep-auditable like everything else. Est. delta +0.004 (and it retires the 4-round "inline emissions" line from every future verdict).

Post-fix estimate: 9.682 + 0.010 + 0.008 + 0.004 = **9.704**, every dimension at the 9.7 gate. Beyond that, the residual is server-side: the 5.52 MB `/api/players` body (single-shot 4.8 s warm, but burst shots 11.8–33.8 s until the Supabase env lands — creds still pending from Travis per the coordination log) and the preseason data ceiling; both outside UI scope.

VERDICT: 9.7/10
